#include <stdio.h>

int main() {
    int n;
    printf("");
    scanf("%d", &n);

    for(int i=0, n<=i, i++){
        for (int j=i, j<=i, j++){
            printf("* /n");
        }
    }
    return 0;
}